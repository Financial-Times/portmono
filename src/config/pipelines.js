const pipelines = {
  /*
  'fdi-article': {
    pipeline: {
      id: process.env.FDI_ARTICLE_HEROKU_PIPELINE_ID
    },
    source: {
      app: {
        id: process.env.FDI_ARTICLE_TEST_HEROKU_APP_ID
      }
    },
    targets: [
      {
        app: {
          id: process.env.FDI_ARTICLE_HEROKU_APP_ID
        }
      }
    ]
  },
  */
}

module.exports = pipelines
